# RetinXplain-dr

Scaffold for the RetinXplain (DR) project. This repository contains a minimal folder layout and small helper stubs. Add model binaries and datasets outside of Git or via Git LFS / cloud storage.

See `requirements.txt` for runtime dependencies and `.env.example` for environment variables.
